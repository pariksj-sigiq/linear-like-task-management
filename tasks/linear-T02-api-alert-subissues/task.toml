version = "1.0"

[metadata]
id = "linear-T02"
name = "API alert sub-issue breakdown"
author_name = "Collinear AI"
category = "linear-clone"
tags = ["cua", "linear", "gui-only", "tier1", "sub-issues", "projects", "cycles"]
difficulty = "L3"
difficulty_explanation = "Creates three child issues that must inherit the right project, cycle, assignee, and parent."
features = ["sub-issues", "projects", "cycles"]
tools = ["create_sub_issue"]
failure_modes = ["multi_step_stateful", "self_referential_linking"]

[environment]
template = "cua-eval-linear-clone"
allow_internet = false
cpus = 4
memory_mb = 6144
storage_mb = 15360

[verifier]
timeout_sec = 90
command = "python tests/verify.py"
