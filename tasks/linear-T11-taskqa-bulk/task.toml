version = "1.0"

[metadata]
id = "linear-T11"
name = "Task QA bug bulk assignment"
author_name = "Collinear AI"
category = "linear-clone"
tags = ["cua", "linear", "gui-only", "tier1", "bulk-operations", "cycles", "issues"]
difficulty = "L3"
difficulty_explanation = "Requires bulk updating exactly three stale task QA bugs with multiple properties."
features = ["bulk-operations", "cycles", "issues"]
tools = ["bulk_update_issues"]
failure_modes = ["bulk_selective_action", "data_dependent_reasoning"]

[environment]
template = "cua-eval-linear-clone"
allow_internet = false
cpus = 4
memory_mb = 6144
storage_mb = 15360

[verifier]
timeout_sec = 90
command = "python tests/verify.py"
