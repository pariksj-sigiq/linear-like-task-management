version = "1.0"

[metadata]
id = "linear-T04"
name = "Customer issue handoff"
author_name = "Collinear AI"
category = "linear-clone"
tags = ["cua", "linear", "gui-only", "tier1", "issue-detail", "labels", "comments", "activity"]
difficulty = "L2"
difficulty_explanation = "Requires updating issue properties and adding a user-visible comment, not only activity."
features = ["issue-detail", "labels", "comments", "activity"]
tools = ["update_issue", "apply_issue_labels", "add_comment"]
failure_modes = ["mode_confusion", "multi_step_stateful"]

[environment]
template = "cua-eval-linear-clone"
allow_internet = false
cpus = 4
memory_mb = 6144
storage_mb = 15360

[verifier]
timeout_sec = 90
command = "python tests/verify.py"
