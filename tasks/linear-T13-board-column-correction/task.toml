version = "1.0"

[metadata]
id = "linear-T13"
name = "Board column correction"
author_name = "Collinear AI"
category = "linear-clone"
tags = ["cua", "linear", "gui-only", "tier1", "board", "relations", "comments"]
difficulty = "L2"
difficulty_explanation = "Requires changing the target board card while preserving the adjacent distractor card."
features = ["board", "relations", "comments"]
tools = ["update_issue", "create_issue_relation", "add_comment"]
failure_modes = ["wrong_column", "self_referential_linking"]

[environment]
template = "cua-eval-linear-clone"
allow_internet = false
cpus = 4
memory_mb = 6144
storage_mb = 15360

[verifier]
timeout_sec = 90
command = "python tests/verify.py"
