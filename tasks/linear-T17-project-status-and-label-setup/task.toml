version = "1.0"

[metadata]
id = "linear-T17"
name = "Project status and label setup"
author_name = "Collinear AI"
category = "linear-clone"
tags = ["cua", "linear", "gui-only", "tier1", "project-statuses", "project-labels"]
difficulty = "L2"
difficulty_explanation = "Requires creating a workspace project status, adding a project label, and applying it to the correct project."
features = ["project-statuses", "project-labels", "projects"]
tools = ["create_project_status", "create_label", "add_project_label", "update_project"]
failure_modes = ["wrong_scope", "metadata_drift"]

[environment]
template = "cua-eval-linear-clone"
allow_internet = false
cpus = 4
memory_mb = 6144
storage_mb = 15360

[verifier]
timeout_sec = 90
command = "python tests/verify.py"
