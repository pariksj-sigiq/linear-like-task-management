version = "1.0"

[metadata]
id = "linear-T12"
name = "Project update from issue comments"
author_name = "Collinear AI"
category = "linear-clone"
tags = ["cua", "linear", "gui-only", "tier1", "projects", "project-updates", "comments"]
difficulty = "L3"
difficulty_explanation = "Requires summarizing issue comments into the project update surface, not the issue comment stream."
features = ["projects", "project-updates", "comments"]
tools = ["update_project", "create_project_update"]
failure_modes = ["data_dependent_reasoning", "mode_confusion"]

[environment]
template = "cua-eval-linear-clone"
allow_internet = false
cpus = 4
memory_mb = 6144
storage_mb = 15360

[verifier]
timeout_sec = 90
command = "python tests/verify.py"
